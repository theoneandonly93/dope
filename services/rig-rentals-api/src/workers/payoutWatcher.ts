// Skeleton for payout watching; integrate chain explorer APIs here.
export async function runPayoutWatcherOnce() {
  // 1. Load wallets from DB
  // 2. Fetch latest txs per address from explorers
  // 3. Record new payouts into a payouts table
  // 4. Optionally trigger conversion and crediting logic
  return { ok: true };
}
